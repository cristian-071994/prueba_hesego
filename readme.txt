crear bd con archivo hesego.sql

usar el comando para instalar el ambiente virual
    pip install virtualenv

ingresar a la carpeta raiz y activar el ambiente virtual
    venv\Scripts\activate

ingresar en la carpeta raiz y ejecutar el comando 
    pip install -r requirements.txt

ingresar al archivo run.py

